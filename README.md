# Assignment 3: Linear Model

Simple linear regression on salary data (`YearsExperience` vs `Salary`).

This repository has two folders:

- `manual/` — see `manual/README.md`
- `ai/` — see `ai/README_AI.md`

Each folder is self-contained with its own data, scripts, notebooks, environment files, and setup instructions.
